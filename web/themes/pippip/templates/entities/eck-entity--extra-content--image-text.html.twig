{% extends "eck-entity--extra-content.html.twig" %}

{% block content %}

  <div class="m-entity__image-text__image-wrapper">
    {{ content.field_image }}
  </div>

  <div class="m-entity__image-text-content">
    <h3>{{ content.title }}</h3>

    {% if content.field_copy|render %}
      {{ content.field_copy}}
    {% endif %}
  </div>
{% endblock %}
