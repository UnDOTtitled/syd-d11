{% if content %}
    <div class="desktop-slider-slide">
        <video muted loop autoplay id="bannerVideo">
            <source src="{{ file_url(content.field_media_video_file["#items"][0].entity.uri.value) }}" type="video/mp4">
        </video>
    </div>
{% endif %}